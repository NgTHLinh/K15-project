using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace DAO
{
    public class Class1
    {/*
        public AccountDAO()
            : base()
        {
        }
        public DataTable getAccount(AccountEntities user)  abc
        {
            string sql = "select * from Account where username='" + user.GetUsername + "' and pass='" + user.GetPass + "'";
            return getDataTable(sql);
        }
        public DataTable getAllAccount()
        {
            string sql = "select * from Account";
            return getDataTable(sql);
        }
        public void updateList(List<AccountEntities> list)
        {
            int i;
            for (i = 0; i < list.Count; i++)
            {
                string sql = "UPDATE Account  set QualityControl=" + list[i].GetQualityControl + ", InventoryReading =" + list[i].GetInventoryReading + ", ModelDatabase=" + list[i].GetModelDatabase + ", EPCTranslator=" + list[i].GetEPCTranslator + " where username='" + list[i].GetUsername + "'";
                ExeCuteNonQuery(sql);
            }
        }
        public void updatePass(AccountEntities acc)
        {
            string sql = "UPDATE Account  set pass='" + acc.GetPass + "' where username='" + acc.GetUsername + "'";
            ExeCuteNonQuery(sql);
        }*/
    }
}
